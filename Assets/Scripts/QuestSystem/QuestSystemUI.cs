using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;
using UnityEngine.UI;

public class QuestSystemUI : MonoBehaviour
{
	//[SerializeField] private QuestManager manager;
	//[SerializeField] private Text questText;

	//private void Start()
	//{
	//	questText = GetComponent<TextMeshProUGUI>();
	//	questText.SetActive(false);
	//}

	//public void DisplayQuest()
	//{
	//	questText.SetActive(true);
	//	questText = $"Get {manager.quests[manager.activeQuest].steps[manager.currentStep].GetCount()} {manager.quests[manager.activeQuest].steps[manager.currentStep].GetDisplayName()} for Many";
	//}
}
